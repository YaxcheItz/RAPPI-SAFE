version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    # Cuando quieras usar Redis para Channels, puedes habilitar esto:
    # depends_on:
    #   - redis

  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
